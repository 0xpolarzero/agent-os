# @rivet-dev/agent-os-posix

POSIX runtime driver for agentOS. This package provides the WASI-based Unix userland layer used by agentOS VMs.

It is primarily intended for internal composition inside agentOS rather than direct end-user use.
